import React from 'react';
import IBill from "../Bills/IBill";

const StatisticsContainer: React.FC = () =>{
    const [bills,setBills] = React.useState<IBill[]>([])

    return (
        <div></div>
    );
}

export default StatisticsContainer;